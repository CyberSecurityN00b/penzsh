source $PENZSH_CMD_DIR/.core

pzcore_func_require "which nmap" "sudo apt-get install nmap"
if [ "$?" = 0 ] ; then
	mkdir -p $pzcore_dir_enum/nmap
	nmap -sUV $pzcore_target -oA $pzcore_dir_enum/nmap/udpscan.$pzcore_timestamp $@
fi
